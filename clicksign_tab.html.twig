<div class="card">
    <div class="card-body">
        <h3>Enviar Chamado para ClickSign</h3>
        <p class="text-muted mb-4">
            Clique no botão abaixo para enviar este chamado para o ClickSign
        </p>
        
        <button 
            type="button" 
            class="btn btn-primary clicksign-send-btn" 
            data-ticket-id="{{ ticket.fields['id'] }}"
            data-webhook-url="{{ webhook_url }}">
            <i class="ti ti-send"></i>
            <span>Enviar</span>
        </button>

        <div id="clicksign-response-{{ ticket.fields['id'] }}" class="mt-3" style="display: none;"></div>
    </div>
</div>